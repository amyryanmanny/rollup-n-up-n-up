{{- set url = getConfig("url") -}}
{{- set issues = await github.url(url) -}}

# [{{ getConfig("title") || "Report" }} {{ today }}]({{ issues.url }})

{{ issues.remember() }}
{{ await memory.summarize("tldr") }}

{{ debugMemory() |> safe }}
