# Synapse Initiative Updates **{{ new Date().toISOString().slice(0, 10) }}**

{{ set synapseInitiatives = await client.issuesForProject("github", 21922, "Initiative", "Project Layer").all() }}

{{ for initiative of synapseInitiatives }}
  ## {{ initiative.title() }}
  {{ set latestUpdate = await initiative.latestUpdate() }}
  {{ latestUpdate.body }}
{{ /for }}
